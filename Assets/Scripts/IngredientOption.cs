using UnityEngine;

public class IngredientOption : MonoBehaviour
{
    public float stackHeightOffset = 20f;

    public int number; // The number label on the ingredient
    public string ingredientType; // e.g., "meat", "cheese"
}
